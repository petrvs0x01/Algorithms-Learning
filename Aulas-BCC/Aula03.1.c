#include <stdio.h>

    /*
    Declarar a Variáveis:
        type  <variable name>;
        - Para utilizar precisamos atribuir valor para as Variáveis
            x = <value int;
        - Podemos declarar e atribuir
            type int x = 5;
    Para realizar o scan de dados em uma aplicação, vamos utilizar scanf
        scanf("%",&<nome da variável)
            - Na prática, quando colocamos o & estamos falando para o programa chamar o endereço da variável na memória RAM.
    */

int main(void){
    int med1;
    int med2;
    
    printf("Digite a primeira nota:")
    scanf(%
    return 0;
}
